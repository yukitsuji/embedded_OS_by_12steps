int a1 = 1; 初期値がある静的変数なのでデータ領域に
static int a2 = 2; 初期値がある静的変数なのでデータ領域に
int a3; 初期値がない静的変数なのでBSS領域に
static int a4; 初期値がない静的変数なのでBSS領域に

int func(){
  int a5; 自動変数なのでスタックへ
  int a6 = 6; 初期値がある自動変数はスタックへ
  static int a7 = 7; 初期値がある静的変数なのでデータ領域へ
  static int a8; 初期値がない静的変数なのでBSS領域へ
}
